import {
    Panel,
} from '@vkontakte/vkui';

const RegistrationPanel = ({id}) => {
    return ( 
    <Panel id={id}>
        <div>registration</div>
    </Panel>);
}

export default RegistrationPanel;